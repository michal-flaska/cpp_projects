#include <iostream>
#include <windows.h>

#include "notes.h"

// just a little note to myself:
// Beep() maxes out at around 32767 Hz,
// and has a lower limit around 37 Hz

int main(){

        // happy birthday
        // Beep(G4, 200);
        // Beep(G4, 200);
        // Beep(A4, 400);
        // Beep(G4, 400);
        // Beep(C5, 400);
        // Beep(B4, 800);

        return 0;
}
